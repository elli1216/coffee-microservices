server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/SimoCs/caffeine-configs
          default-label: main
  application:
    name: config-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
